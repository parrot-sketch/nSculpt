/**
 * FollowUpPlanStatus enum
 * 
 * Matches the enum defined in Prisma schema (follow-up-plan.prisma)
 */
export enum FollowUpPlanStatus {
  PENDING = 'PENDING',
  SCHEDULED = 'SCHEDULED',
  COMPLETED = 'COMPLETED',
  CANCELLED = 'CANCELLED',
}
